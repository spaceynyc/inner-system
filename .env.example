# Production only. Local dev uses embedded PostgreSQL in .data/postgres.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require
# Generate random values; never expose these with a VITE_ prefix.
SHARE_SIGNING_KEY=
CRON_SECRET=
APP_ORIGIN=https://inner-system-two.vercel.app
